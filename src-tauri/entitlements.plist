<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened-runtime entitlements for notarized (non-sandboxed) distribution.
      The Python sidecar (torch / whisperX / pyannote) loads unsigned native
      libraries and JITs code, both of which the hardened runtime blocks by
      default. These exceptions keep that stack working under notarization.
      None of these are App Sandbox keys: that re-architecture is Phase 2 (MAS).
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
